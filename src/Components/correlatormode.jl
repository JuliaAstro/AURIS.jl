struct CorrelatorMode
    correlatorModeID
    numBaseband
    basebandNames
    basebandConfig
    accumMode
    binMode
    numAxes
    axesOrderArray
    filterMode
    correlatorName
    raw   # complete original row (lossless catch-all)
end
